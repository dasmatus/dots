# tokyonight-dots Gentoo installer package.
# Entry point is main.py (run as a script, not via `-m` — the modules use flat
# imports so the same files work from the repo checkout, /root/installer/, and
# inside the chroot at /root/installer/).
