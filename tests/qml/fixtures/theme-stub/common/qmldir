# Every component here must be listed, not only the singleton — the same
# rule the shipped nix/home/desktop/quickshell/qml/common/qmldir records having been
# bitten by. Without the file the engine resolves `pragma Singleton` on
# Tokens only after a first pass where every Tokens reference reads
# undefined, which is noisy rather than wrong.
#
# Pill.qml is listed as of tst_settings_controls.qml: ChipRow.qml builds its
# chips on it, the first fixture consumer. PopupShell.qml stays absent below
# — still no test drives it — a fixture exports what it has symlinked, not
# what the shipped directory holds.
singleton Tokens 1.0 Tokens.qml
Chrome 1.0 Chrome.qml
EdgeStrip 1.0 EdgeStrip.qml
Field 1.0 Field.qml
Panel 1.0 Panel.qml
Pill 1.0 Pill.qml
