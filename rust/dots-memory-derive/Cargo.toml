[package]
name = "dots-memory-derive"
version = "0.1.0"
edition = "2021"
description = "mechanical extractor: repo structure to derived-origin Mermaid edges"
license = "AGPL-3.0-only"

[[bin]]
name = "dots-memory-derive"
path = "src/main.rs"

[dependencies]
regex = "1"

[profile.release]
lto = true
strip = true
