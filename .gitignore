.codegraph/
.config
__pycache__/

# VM test harness — generated/downloaded artifacts (never committed):
# qcow2 disks, cached ISOs, per-run OVMF _VARS copies, swtpm state,
# serial logs, captured lsblk/luksDump output, local test signing keys.
tests/artifacts/
*.qcow2

# Nix + installer-tui build outputs
installer-tui/target/
result
result-*

# Secure Boot signing keys (scripts/sign-iso.sh). The private key must never
# be committed; the public MOK.cer may be force-added if wanted.
secrets/
